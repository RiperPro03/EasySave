name: CI

on:
  pull_request:
    branches: [ "main" ]
  push:
    branches: [ "main" ]
    tags: [ "v*.*.*" ]

env:
  DOTNET_VERSION: "8.0.x"
  CONFIGURATION: "Release"

jobs:
  # -------------------------
  # STAGE 0 — Detect changes
  # -------------------------
  detect_changes:
    name: "Stage 0 - Detect changes"
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    outputs:
      code_changed: ${{ steps.filter.outputs.code }}
    steps:
      - uses: actions/checkout@v4

      - name: "Detect code changes"
        uses: dorny/paths-filter@v3
        id: filter
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          filters: |
            code:
              - "src/**"
              - "tests/**"
              - "**/*.sln"
              - "**/*.csproj"
              - "**/*.cs"
              - "Directory.Build.props"
              - "Directory.Build.targets"
              - ".editorconfig"

  # -------------------------
  # STAGE 1 — Unit tests
  # -------------------------
  unit_tests:
    name: "Stage 1 - Unit tests"
    needs: detect_changes
    if: ${{ needs.detect_changes.outputs.code_changed == 'true' }}
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: "Setup .NET"
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}
          
      - name: Cache NuGet
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: nuget-${{ runner.os }}-${{ hashFiles('**/*.csproj') }}
          restore-keys: |
            nuget-${{ runner.os }}-

      - name: "Restore"
        run: dotnet restore

      - name: "Test"
        run: dotnet test -c ${{ env.CONFIGURATION }} --verbosity normal

  # -------------------------
  # STAGE 2 — Build (only if tests OK)
  # -------------------------
  build:
    name: "Stage 2 - Build"
    needs: [detect_changes, unit_tests]
    if: ${{ needs.detect_changes.outputs.code_changed == 'true' }}
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: "Setup .NET"
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}
          
      - name: Cache NuGet
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: nuget-${{ runner.os }}-${{ hashFiles('**/*.csproj') }}
          restore-keys: |
            nuget-${{ runner.os }}-

      - name: "Restore"
        run: dotnet restore

      - name: "Build"
        run: dotnet build -c ${{ env.CONFIGURATION }} --no-restore

# -------------------------
  # STAGE 3 — SonarCloud (ONLY ON TAG RELEASE)
  # -------------------------
  sonarcloud:
    name: "Stage 3 - SonarCloud (on release tag)"
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: "SonarCloud Scan"
        uses: SonarSource/sonarcloud-github-action@v3
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  # -------------------------
  # STAGE 4 — Package (.exe) (ONLY ON TAG RELEASE)
  # -------------------------
  package:
    name: "Stage 4 - Package (.exe)"
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: "Setup .NET"
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: "Publish (win-x64)"
        run: >
          dotnet publish src/EasySave.App.Console/EasySave.App.Console.csproj
          -c ${{ env.CONFIGURATION }}
          -r win-x64
          --self-contained false
          /p:PublishSingleFile=true
          /p:DebugType=None
          -o ./out/publish

      - name: "Zip artifact"
        shell: pwsh
        run: |
          Compress-Archive -Path out/publish/* -DestinationPath EasySave-win-x64.zip -Force

      - name: "Upload artifact"
        uses: actions/upload-artifact@v4
        with:
          name: EasySave-win-x64
          path: EasySave-win-x64.zip

  # -------------------------
  # STAGE 5 — GitHub Release (attach zip) (ONLY ON TAG RELEASE)
  # -------------------------
  release:
    name: "Stage 5 - GitHub Release"
    needs: [package, sonarcloud]
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: "Download artifact"
        uses: actions/download-artifact@v4
        with:
          name: EasySave-win-x64

      - name: "Create GitHub Release"
        uses: softprops/action-gh-release@v2
        with:
          files: EasySave-win-x64.zip

  # -------------------------
  # STAGE SKIP — Docs only => green pipeline
  # -------------------------
  docs_only_ok:
    name: "Stage - Docs only (skip)"
    needs: detect_changes
    if: ${{ needs.detect_changes.outputs.code_changed != 'true' }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Docs-only change -> no tests/build. Pipeline validated"
